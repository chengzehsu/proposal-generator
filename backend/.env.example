# ========================================
# 智能標案產生器 - 環境變數範本
# ========================================
#
# 🔧 使用說明：
# 1. 本地開發：複製此檔案為 .env 並填入開發環境的值
# 2. 生產環境：使用 Zeabur CLI 設定環境變數
#
# 🚀 Zeabur 部署環境變數設定指令：
# zeabur env set JWT_SECRET=<your-production-secret>
# zeabur env set GEMINI_API_KEY=<your-gemini-key>
# ========================================

# 資料庫設定
# 本地開發：file:./dev.db (SQLite)
# 生產環境：Zeabur 會自動設定 DATABASE_URL（如使用 Zeabur PostgreSQL）
DATABASE_URL="file:./dev.db"

# JWT 設定
# ⚠️ 重要：生產環境必須使用強隨機密鑰！
# 生成指令：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Zeabur 設定：zeabur env set JWT_SECRET=<generated-key>
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Gemini AI 設定
# 取得 API Key: https://makersuite.google.com/app/apikey
# Zeabur 設定：zeabur env set GEMINI_API_KEY=<your-key>
GEMINI_API_KEY="your-gemini-api-key"

# 伺服器設定
PORT=3001
NODE_ENV=development

# 檔案上傳設定
UPLOAD_MAX_SIZE="10MB"
UPLOAD_DIR="./uploads"

# CORS 設定
# 本地開發：http://localhost:5173
# 生產環境：https://your-frontend-domain.zeabur.app
# Zeabur 設定：zeabur env set FRONTEND_URL=<your-frontend-url>
FRONTEND_URL="http://localhost:5173"

# 速率限制設定
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# 日誌設定
LOG_LEVEL="info"
LOG_DIR="./logs"

# Redis 設定 (選用)
REDIS_URL="redis://localhost:6379"

# 安全設定
BCRYPT_ROUNDS=12